# Bad config test case
# Missing objective, optimizer, and contains invalid input
input_size: 0.5 # Should be int
layers:
- layer:
    activation: r2lue # Should be relu
    dropout: false
    neurons: 1
    type: linear" # Should be linear
model_type: fully-connected
number_of_layers: 2 # Should be 1
